#!/bin/bash

user=$(whoami);

PATTERNS_PATH=$HOMESERVER_PATH/storage/app/patterns;

HOMESERVERPATH=/usr/share/homeserver;

MYSQLPASSWORD=$(openssl rand -hex 16);

HOMESERVER_LOG_PATH=/var/log/homeserver
