APP_NAME=Homeserver
APP_ENV={env}
APP_KEY={app_key}
APP_DEBUG=false
APP_URL=https://{fqdn}
MIX_APP_URL=https://www.{fqdn}

LOG_CHANNEL=stack
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=homeserver
DB_USERNAME=homeserver
DB_PASSWORD={mysqlpassword}

BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DRIVER=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120
